services:
  - type: static
    name: the-quiet-edit
    buildCommand: npm install && npm run build
    staticPublishPath: dist/client
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
